name: to_do_app_flutter_tutorial_hive_local_storage_by_mitch_koko
description: "A fully functional TODO app made with Flutter! I used Hive for the local storage back-end."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
